Readability first.
Write clean, simple, easy to understand code. No duplicate code.
Instead of repeating yourself, create functions, classes, or modules to encapsulate reusable logic.
Don't build features before they're needed, avoid speculative generality; complexity only when required.
Start simple, refactor when needed.
